# Backend database connection (SQLModel)
DATABASE_URL=postgresql+asyncpg://unoc:unocpw@postgres:5432/unocdb

# Backend server
UNOC_PORT=5001
UNOC_SHUTDOWN_TOKEN=dev-token
UNOC_DEV_FEATURES=1

# Optional feature flags (reserved for Phase 4/5 work)
USE_GO_TRAFFIC=0
USE_PORT_SUMMARY_SERVICE=0

# Logging (set to DEBUG/INFO/WARNING/ERROR)
UNOC_LOG_LEVEL=INFO

# Frontend build host (used for reverse proxy configuration scripts)
FRONTEND_BASE_URL=http://localhost:5173
